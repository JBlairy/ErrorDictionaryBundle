<!DOCTYPE html>
<html>
<head>
    {% block meta %}
        <meta charset="UTF-8">
    {% endblock meta %}
    <title>{% block title %}Error Dictionary{% endblock title %}</title>
</head>
<body>
{% for error, description in data %}
    <div><span>{{ error }}</span> {{ description }}</div>
{% endfor %}
</body>
</html>
